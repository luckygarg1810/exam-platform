# ─────────────────────────────────────────────────────────────────
# GBU Exam Platform — Environment Variables
# Copy to .env and fill in your values before running docker-compose
# ─────────────────────────────────────────────────────────────────

# ── pgAdmin ───────────────────────────────────────────────────────
PGADMIN_EMAIL=admin@exam.com
PGADMIN_PASSWORD=change_me_pgadmin_password

# ── PostgreSQL ────────────────────────────────────────────────────
DB_NAME=examdb
DB_USER=examuser
DB_PASSWORD=change_me_strong_password
# Override only when not using docker-compose networking (default: localhost:5432)
#DB_HOST=localhost
#DB_PORT=5432

# ── Redis ─────────────────────────────────────────────────────────
REDIS_PASSWORD=change_me_redis_password
# Override only when not using docker-compose networking (default: localhost:6379)
#REDIS_HOST=localhost
#REDIS_PORT=6379

# ── RabbitMQ ──────────────────────────────────────────────────────
RABBITMQ_USER=examuser
RABBITMQ_PASSWORD=change_me_rabbitmq_password
# Override only when not using docker-compose networking (default: localhost:5672)
#RABBITMQ_HOST=localhost
#RABBITMQ_PORT=5672

# ── MinIO ─────────────────────────────────────────────────────────
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=change_me_minio_secret_key
# Override when MinIO is not on the default local port (default: http://localhost:9000)
# In docker-compose deployments this is overridden to http://minio:9000 automatically.
#MINIO_ENDPOINT=http://localhost:9000

# ── JWT ───────────────────────────────────────────────────────────
# REQUIRED: Must be at least 64 characters for HS512. The app will reject the
# built-in placeholder on non-dev profiles — replace with a real random secret.
JWT_SECRET=change_me_replace_with_a_random_64plus_character_secret_for_hs512_security
JWT_ACCESS_EXPIRY=3600000      # 1 hour in milliseconds
JWT_REFRESH_EXPIRY=604800000   # 7 days in milliseconds

# ── Mail ──────────────────────────────────────────────────────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# ── AI Service ────────────────────────────────────────────────────
# URL the backend uses to reach the Python AI service.
# In docker-compose deployments this is overridden to http://ai-service:8000 automatically.
#AI_SERVICE_URL=http://localhost:8000

# ── Application ───────────────────────────────────────────────────
# Public base URL of the frontend — used in password-reset and notification emails.
#APP_BASE_URL=http://localhost:3000
